const canvas=document.getElementById('starfield'),ctx=canvas.getContext('2d');let stars=[];function resize(){canvas.width=innerWidth*devicePixelRatio;canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);stars=Array.from({length:Math.min(180,Math.floor(innerWidth/7))},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,r:Math.random()*1.2+.2,a:Math.random()*.7+.15,v:Math.random()*.08+.02}))}function draw(){ctx.clearRect(0,0,innerWidth,innerHeight);for(const s of stars){s.y+=s.v;if(s.y>innerHeight)s.y=0;ctx.beginPath();ctx.arc(s.x,s.y,s.r,0,Math.PI*2);ctx.fillStyle=`rgba(120,205,255,${s.a})`;ctx.fill()}requestAnimationFrame(draw)}addEventListener('resize',resize);resize();draw();
const events=[['00:00:01','Genesis Network 0001 initialized','VERIFIED'],['00:00:04','Protocol invariants published','VERIFIED'],['00:00:07','Identity specification registered','SPECIFIED'],['00:00:12','Family Graph specification registered','SPECIFIED'],['00:00:18','Contribution Ledger entered build phase','BUILDING'],['00:00:24','MARS issuance gate remains locked','DEFERRED']];document.getElementById('eventFeed').innerHTML=events.map(e=>`<div class="event"><time>${e[0]}</time><b>${e[1]}</b><span>${e[2]}</span></div>`).join('');
const modal=document.getElementById('protocolModal');document.querySelectorAll('[data-open]').forEach(b=>b.addEventListener('click',()=>{modal.classList.add('open');modal.setAttribute('aria-hidden','false')}));document.querySelector('.close').addEventListener('click',()=>{modal.classList.remove('open');modal.setAttribute('aria-hidden','true')});modal.addEventListener('click',e=>{if(e.target===modal)document.querySelector('.close').click()});document.addEventListener('keydown',e=>{if(e.key==='Escape')document.querySelector('.close').click()});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.animate([{opacity:0,transform:'translateY(18px)'},{opacity:1,transform:'translateY(0)'}],{duration:650,easing:'cubic-bezier(.2,.7,.2,1)',fill:'both'})}),{threshold:.08});document.querySelectorAll('.module,.panel,.stack>div').forEach(el=>observer.observe(el));